{
  "name": "aurethica-api",
  "version": "1.0.0",
  "description": "Auréthica API backend service",
  "private": true,
  "scripts": {
    "dev": "cd api && npm start",
    "start": "cd api && npm start",
    "install:api": "cd api && npm install"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}